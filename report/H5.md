
```
#     #    #######
#     #    #
#     #    #   
#######    #######
#     #          #
#     #          #
#     #    #######
```
`https://terokarvinen.com/palvelinten-hallinta/` 

| core | ram |  vm/os      |
|------|-----|-------------|
| 12   | 64  |  Fedora     |
|4     | 8   |  Debian     |


# X  
# 1. Pro Git
- You can start using Git by: `git init`
- Cloning an existing repo: `git clone <url>`
- To add files (`git add`) and commit them (`git commit`)

---



- Files have two states:
  - Tracked (known to Git)
  - Untracked (not yet added)

### Workflow:
1. Modify files  
2. Stage changes (`git add`)  
3. Commit changes (`git commit`)  

### Key commands:

```
git status
```

→ shows file states

```
git diff
```

→ shows exact changes

### Other actions:
- Remove files: `git rm`
- Rename/move files: `git mv`

---



- `git log` shows commit history

### Useful options:

```
-p → show diffs
--oneline → compact view
--since / --until → date filtering
--author → filter by author
--grep → search commit message
-S → search content changes
```

---



### Amend last commit:
- `git commit --amend`

### Discard local changes:
- `git checkout -- <file>` or `git restore <file>`

!!! This deletes uncommitted work permanently !!!

---



Remotes are versions of your repo hosted elsewhere.

### View remotes:

```
git remote
git remote -v
```

### Add/remove remotes:

```
git remote add <name> <url>
git remote remove <name>
git remote rename <old> <new>
```

### Fetching vs Pulling:

- `git fetch <remote>`
→ downloads changes without merging

- `git pull`
→ fetches and merges automatically

### Pushing:

- `git push <remote> <branch>`
→ uploads your commits

### Deleting tags:

```
Local: git tag -d <tag>
Remote: git push origin --delete <tag>
```

---


- Work with local repositories  
- Track and commit changes  
- View history  
- Collaborate via remotes (push, pull, fetch)  
- Mark releases with tags  
- Customize workflow with aliases  

---

# 2 Getting Started – What is Git?

### Core idea
- Git is a version control system that keeps track of changes in your project. Instead of storing differences between files like older systems, it saves snapshots of the whole project over time.

### Key concept
- Each commit is basically a snapshot of all your files at that moment.
- If something hasn’t changed, Git just points to the existing version instead of saving it again, so it stays efficient.

### Mostly local operations
- Git keeps the entire project history on your machine.
- That means you can view history, compare changes, and make commits without internet access.
- You only need to connect when you want to push or pull changes.

### Data is rarely lost
- Git is designed to mostly add data rather than delete it.
- Once something is committed, it’s usually very difficult to lose.

### States of files
1. Modified – you’ve changed the file, but Git hasn’t saved it yet  
2. Staged – the file is marked to be included in the next commit  
3. Committed – the file is saved in Git’s history

---

# A)

## Prereq

```
sudo dnf update
sudo dnf install git gh
```
<img width="1711" height="398" alt="image" src="https://github.com/user-attachments/assets/deb5bfd5-10e2-4636-a90f-f10cb69e3906" />



(I have used GitHub from a terminal before and I used SSH. Now I'll use HTTPS)

```
gh auth login
```
 

Note: Entered a one-time password.  
  
<img width="675" height="102" alt="image" src="https://github.com/user-attachments/assets/69a9d7ad-2c65-43e5-b3f8-3b28c144b844" />

✓ Logged in as Kip-up

---

##  Making repo
```
mkdir sunshine-repo
git init
```

```
gh repo create sunshine-repo \ #[1, 2, 4]
  --public \
  --description "sunshine coursework H5 repository" \ #[3]
  --license gpl-3.0 \ #[5.2]

echo "sunshine coursework H5 repository" > README.md #[5.1]

git add README.md
git commit -m "initial README"
```
### check
1. [X] 1. Create a new repository on Git   
2. [X] 2. Repository name includes the word **"sunshine"**  
3.  [X] 3. Repository description includes the word **"sunshine"**  
4.  [X] 4. Do NOT reuse an existing repo
5.  [X] 5. Initialize repository with files:
  5.1  [X] 5.1`README.md`
  5.2  [X] 5.2 `License file: **GNU General Public License v3**`  
6.  [X] 6.Confirm the repository exists online and is accessible
<img width="1109" height="668" alt="image" src="https://github.com/user-attachments/assets/d2e02ba4-0143-4740-addf-4c2e2b464d69" />
<img width="1225" height="302" alt="image" src="https://github.com/user-attachments/assets/8ebaed48-e445-453d-98d6-fba93f58cd24" />



---

# B)

## Clone

```
gh repo clone Kip-up/sunshine-repo # [1]
```

```
cd sunshine-repo
nano README.md #[2]
```
<img width="721" height="148" alt="image" src="https://github.com/user-attachments/assets/65c25fb8-eee5-4628-b1e3-bc06c5b31359" />



##### cat README.md

```
# sunshine-repo

sunshine coursework H5 repository `Edited locally`
```

```
git add README.md #[3]
git commit -m "local edit" #[4]
git push #[5]
```
##[6]
<img width="721" height="186" alt="image" src="https://github.com/user-attachments/assets/9427338f-2293-4128-b7e1-81837f7831f7" />
<img width="1255" height="333" alt="image" src="https://github.com/user-attachments/assets/10730040-3c83-468b-a5ca-8e353e816ae8" />


---
### Check

- [X] 1. Clone the repository   
- [X] 2. Make changes locally 
- [x] 3. Stage changes using `git add`  
- [X] 4. Commit changes using `git commit`  
- [X] 5. Push changes using `git push`  
- [X] 6. Verify in web interface  
---

# C) Doh!

### Making and destroying a bad uncommitted change

I first made a change to the README file:

```
nano README.md
```

##### cat README.md:

```
# sunshine-repo
really secret, should not be published  #[1]
sunshine coursework H5 repository `Edited locally`
```

This change is in **uncommitted state**, so was not saved in Git history. #[2]

---

### Destroying the bad change

Since the change was not committed, I can reset it to the last committed state:


```
git reset --hard #[3]
```
<img width="628" height="143" alt="image" src="https://github.com/user-attachments/assets/650cdf77-4136-4d98-8dc8-96abd67c5aa9" />


---

## Result


##### cat README.md #[4]

```
# sunshine-repo

sunshine coursework H5 repository `Edited locally`
```
---

### Check
- [x] 1. Make an “bad” change locally  
- [x] 2. DO NOT commit the change  
- [x] 3. Run `git reset --hard`  
- [x] 4. Confirm the changes are removed  

---




# D)

## Looogs
```
git log #[1]
```
```
commit ed9a26725677d91fa64fb7e060ac40d6884f5b7a (HEAD -> main, origin/main, origin/HEAD)
Author: kip-up <blurrrr>
Date:   Sat Apr 25 23:20:01 2026 +0300

    local edit

commit 325d8cf3a4e7d2ca63c73ede99d626cb28bde27e
Author: Kip-up <Blurrrr>
Date:   Sat Apr 25 22:38:34 2026 +0300

    Initial commit

    Initial commit
```

From the log:
- There are 2 commits in total
- First commit is the initial project setup
- summay = create → edit once

---

## Last update

```
git show # [1]
```

```
commit ed9a26725677d91fa64fb7e060ac40d6884f5b7a (HEAD -> main, origin/main, origin/HEAD)
Author: kip-up <blurrrrr>
Date:   Sat Apr 25 23:20:01 2026 +0300

    local edit

diff --git a/README.md b/README.md
index 444d269..3ef14a2 100644
--- a/README.md
+++ b/README.md
@@ -1,2 +1,3 @@
 # sunshine-repo
-sunshine coursework H5 repository
+
+sunshine coursework H5 repository `Edited locally`
```

From the last commit:
- README file was modified
- one line was updated with “Edited locally”
- nothing else in the repo changed

So you can see I did edit to the RADME file and added `Edited locally` 

---
### Check
- [x] 1. Check history  
  
  
- 
---



# E) Git and Ansible Setup

## Prereqs

First, I needed to access another desktop, so I SSH’d into that machine.

Then I ran the following commands inside my Ansible folder:

```
sudo apt-get install git
sudo apt-get install gh
gh auth login
```

- Logged in as `Kip-up`
- Configured Git identity:

```
git config --global user.name "Kip-up"
git config --global user.email "my@Email"
```

---

## Git Init

Ran:

```
git init
echo "### Infrastructure as Code project using Ansible" > README.md
```

---

## Created Folder for all the ansible Code

```
mkdir code
mv adduser.yml ansible.cfg baseline.yml firewall.yml hosts.ini unifi.yml roles code/
```

---

## License Creation

```
curl -o LICENSE https://www.gnu.org/licenses/gpl-3.0.txt
git add LICENSE
git commit -m "Add GPL-3.0 license"
```

---

## GitHub Repository Creation and Push

```
gh repo create infra-as-code \ #[1]
  --public \
  --description "Infrastructure as Code with Ansible " \
  --source=. \
  --remote=origin \
  --push
```
- I don't understand why it says the license was added 40min earlier. I pushed it all the same time.
```Push changes to rem
Note:
It is a commit timestamp. Just haven't noticed it before.
End note
```
<img width="1130" height="674" alt="image" src="https://github.com/user-attachments/assets/795573af-7c05-46c3-81c5-29382baf8d90" />


---

### Changes

```
rm code/roles/unifi/tasks/main.yml.og
rm code/roles/unifi/tasks/main.yml' #[2]
```
```
Note:
git rm might have been better
End note
```

### Committing changes:

```
git add -A #[3]
git commit -m "Delete code/roles/unifi/tasks/main.yml.og backup file"[4]

```
<img width="455" height="151" alt="image" src="https://github.com/user-attachments/assets/7de4e20b-7592-4924-bd3e-e586a40a8d88" />

```
git push
```


## verify
<img width="455" height="151" alt="image" src="https://github.com/user-attachments/assets/4ff8ecb0-b4d1-42b9-8bac-ed2744730bd2" />

--- 
### Check
- [x] 1. Add an `Ansible` directory to the repository  
- [x] 2. Make a change in Ansible configuration   
- [x] 3. Stage changes (`git add`)  
- [x] 4. Commit changes (`git commit`)  
- [x] 5. Push changes to remote repository 


---

# f) 

Done

---

## sources

Pro Git, 2nd ed:  
https://git-scm.com/book/en/v2  

1.3 Getting Started – What is Git?  
https://git-scm.com/book/en/v2/Getting-Started-What-is-Git%3F  

Terokarvinen 2026:
https://terokarvinen.com/palvelinten-hallinta/


